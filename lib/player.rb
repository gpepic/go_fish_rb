class Player
  attr_accessor(:name, :hand, :books)

  def initialize(name, hand)
    @name = name
    @hand = hand
    @books = 0
  end

  def find_books
    count = 0
    hand.each do |card|
      if hand.(card) ==

    .count { |card| card == 0 } #=> 3

    # a.each do |letter|
  # if a.count(letter) == 4
  #   a.delete(letter)
  # end

end
